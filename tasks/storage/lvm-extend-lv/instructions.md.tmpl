# Storage Task

Task: The logical volume `{{lv_name}}` in volume group `{{vg_name}}` is formatted with `{{filesystem}}` and mounted at `{{mount_point}}`.

Extend the logical volume to `{{target_size_mib}} MiB` and grow the filesystem to use all of the new space. The filesystem must remain mounted while you resize it, and the additional space must be available immediately.

Do not modify any other disk.
