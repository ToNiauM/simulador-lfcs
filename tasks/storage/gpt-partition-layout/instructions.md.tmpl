# Storage Task

Task: Partition the disk `{{target_disk}}` using a GPT partition table with exactly two partitions:

1. The first partition must be `{{part1_size_mib}} MiB` in size.
2. The second partition must be `{{part2_size_mib}} MiB` in size.

Leave the rest of the disk unpartitioned. Do not modify any other disk.
