# Storage Task

Task: Create a GPT partition table on `{{target_disk}}` with a single partition of `{{part_size_mib}} MiB`.

Format the partition with the `{{filesystem}}` filesystem and mount it at `{{mount_point}}`.

The mount must persist across reboots. Do not modify any other disk.
