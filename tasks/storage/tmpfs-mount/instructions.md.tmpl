# Storage Task

Task: Configure a tmpfs filesystem mounted at `{{mount_point}}` with a maximum size of `{{size_mib}}` MiB and with the permissions of the mount's root directory set to mode `{{mode}}`.

The mount must be active now and must persist across reboots. Do not modify any disks.
