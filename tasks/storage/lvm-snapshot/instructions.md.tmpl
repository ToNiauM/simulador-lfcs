# Storage Task

Task: The volume group `{{vg_name}}` contains the logical volume `{{origin_lv}}`.

Create an LVM snapshot of `{{origin_lv}}` named `{{snap_name}}`, allocating `{{snap_size_mib}} MiB` for the snapshot.

Do not modify the origin volume and do not touch any other disk.
