# Essential Commands

Task: The directory `{{work_dir}}` contains the data file `{{source_file}}`. Its columns are separated by the character `{{delimiter}}`.

Extract columns `{{fields}}` (in that order, counting from 1) from every line of `{{source_file}}`, including the header line, and write the result to `{{work_dir}}/{{output_file}}`. The extracted columns must keep `{{delimiter}}` as the separator and the lines must stay in their original order.

Do not modify `{{source_file}}`.
