# Essential Commands

Task: The program `{{program_path}}` writes diagnostic messages to both standard output and standard error.

Run `{{program_path}}` exactly once so that its standard output is saved to `{{output_dir}}/{{stdout_file}}` and its standard error is saved to `{{output_dir}}/{{stderr_file}}`. Create `{{output_dir}}` if it does not exist.

Each file must contain only the messages from its own stream, complete and unmodified. Do not modify `{{program_path}}`.
