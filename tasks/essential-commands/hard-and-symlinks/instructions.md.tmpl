# Essential Commands

Task: The directory `{{work_dir}}` contains the files `{{hard_target}}` and `{{sym_target}}`.

1. Create a HARD link at `{{hard_link}}` pointing to the file `{{hard_target}}`.
2. Create a SYMBOLIC link at `{{sym_link}}` pointing to the file `{{sym_target}}`.

Do not modify the contents of the existing files. Each link must be of the exact type requested.
