# Essential Commands

Task: The directory tree `{{tree_dir}}` currently belongs to `root`. The user `{{owner_user}}` and the group `{{owner_group}}` already exist.

Change the ownership of `{{tree_dir}}` and of EVERYTHING beneath it (all subdirectories and files, recursively) so that:

1. the owning user is `{{owner_user}}`;
2. the owning group is `{{owner_group}}`.

Do not change file permissions or contents.
