# Essential Commands

Task: The service configuration file `{{conf_file}}` uses the `key=value` format, one entry per line.

Edit the file in place so that:

1. the key `port` is set to `{{new_port}}`;
2. the key `log_level` is set to `{{new_log_level}}`.

All other keys, values and comment lines must remain exactly as they are. Do not create backup copies inside `/etc` and keep the file at the same path.
