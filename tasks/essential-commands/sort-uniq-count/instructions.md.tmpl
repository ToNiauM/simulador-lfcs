# Essential Commands

Task: The file `{{data_file}}` contains one username per line; usernames repeat.

Produce a frequency report at `{{report_file}}`: one line per distinct username in the format `<count> <username>` (occurrence count, whitespace, username), sorted by count in DESCENDING order.

Each username must appear exactly once in the report and every distinct username of the input must be present. Do not modify `{{data_file}}`.
