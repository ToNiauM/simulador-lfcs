# Essential Commands

Task: The directory `{{work_dir}}` contains the log files `{{gzip_file}}` and `{{xz_file}}`.

Create a gzip-compressed copy of `{{gzip_file}}` named `{{gzip_file}}.gz` and an xz-compressed copy of `{{xz_file}}` named `{{xz_file}}.xz`, both in `{{work_dir}}`.

The original files `{{gzip_file}}` and `{{xz_file}}` must remain in `{{work_dir}}` with their content unchanged. Each compressed file must be in the real format its extension indicates and must decompress to exactly the content of its original file.
