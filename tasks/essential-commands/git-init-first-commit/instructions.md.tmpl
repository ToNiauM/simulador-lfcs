# Essential Commands

Task: The directory `{{repo_dir}}` contains the project files `{{file1_name}}` and `{{file2_name}}`. It is not under version control yet.

Turn `{{repo_dir}}` into a Git repository. Configure, for this repository only (not globally), the identity `user.name` = `{{author_name}}` and `user.email` = `{{author_email}}`.

Then create the repository's first commit containing both project files, authored with that identity, with exactly this commit message:

```
{{commit_message}}
```

Do not modify the content of the project files.
