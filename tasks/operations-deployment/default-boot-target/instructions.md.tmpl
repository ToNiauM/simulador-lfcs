# Task: Set the default boot target

This server is currently configured to boot into `{{initial_target}}`.

Configure the system so that it boots into `{{expected_target}}` by default. The change must persist across reboots.
