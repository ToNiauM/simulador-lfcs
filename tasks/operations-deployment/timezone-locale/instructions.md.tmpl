# Task: Configure timezone and system locale

1. Set the system timezone to `{{timezone}}`.
2. Generate the locale `{{locale}}` and set it as the system default locale (`LANG`) in `/etc/default/locale`.

Both settings must persist across reboots. Do not remove locales that are already installed.
