# Task: System-wide environment variable

Configure a system-wide environment variable named `{{var_name}}` with the value `{{var_value}}`, so that it is set in the environment of every user's login shell.

Implement it as the executable shell script `{{script_path}}`. The script must have valid shell syntax, and the configuration must persist across reboots.
