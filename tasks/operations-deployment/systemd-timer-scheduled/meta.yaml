schema_version: 1
id: operations-deployment/systemd-timer-scheduled
version: 1.0.0
domain: operations-deployment
weight: 10
estimated_minutes: 8
requires:
  - systemd
parameters:
  timer_name: seed-derived base name for the timer/service pair
  script_path: seed-derived path of the maintenance script provided by setup
  on_calendar: seed-derived OnCalendar expression (daily at HH:MM)
