# Task: Schedule a cron job for a user

The user `{{username}}` exists on the system, and a report script has been installed at `{{script_path}}`.

Configure the personal crontab of the user `{{username}}` so that `{{script_path}}` is executed every day at {{hour}}:{{minute}} (24-hour clock, minute {{minute}} of hour {{hour}}).

The job must be listed by the user's crontab and must persist across reboots.
