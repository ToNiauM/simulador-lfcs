schema_version: 1
id: users-groups/system-service-account
version: 1.0.0
domain: users-groups
weight: 10
estimated_minutes: 5
requires:
  - passwd
parameters:
  username: system account name derived from seed
  uid: system UID in 300-899 derived from seed
  home_dir: /var/lib/<username>
