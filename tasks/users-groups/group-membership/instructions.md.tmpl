# Task: Configure supplementary group membership

The user accounts `{{user_one}}` and `{{user_two}}` already exist on this system.

1. Create the groups `{{group_a}}` and `{{group_b}}`.
2. Make `{{user_one}}` a member of both `{{group_a}}` and `{{group_b}}`.
3. Make `{{user_two}}` a member of `{{group_a}}` only.

The new memberships must be supplementary (secondary) memberships: the primary group of each user must remain unchanged.
