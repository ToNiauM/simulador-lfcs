schema_version: 1
id: users-groups/skel-onboarding
version: 1.0.0
domain: users-groups
weight: 10
estimated_minutes: 5
requires:
  - passwd
parameters:
  skel_file: filename to create under /etc/skel, derived from seed
  skel_content: exact single-line content derived from seed
  username: new user to create after populating /etc/skel
