# Networking task

Task: Enable IPv4 packet forwarding on this system. The setting must be in effect immediately and must persist across reboots. Place the persistent configuration in the file `{{sysctl_file}}`.

Do not edit `/etc/sysctl.conf` or any pre-existing file under `/etc/sysctl.d`.
