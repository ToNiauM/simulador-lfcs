# Networking task

Task: The interface `{{lab_nic}}` is already configured with the static address `{{lab_ip}}/{{prefix_len}}`. Configure custom DNS resolvers for this interface: the nameservers `{{dns_server_1}}` and `{{dns_server_2}}`, and the search domain `{{search_domain}}`. The resolvers must be active for the interface now and the configuration must persist across reboots.

Do not modify the configuration of the primary network interface or the default route, and do not modify or delete any configuration files you did not create.
