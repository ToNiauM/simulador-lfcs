# Networking task

Task: Configure the OpenSSH server daemon to listen on TCP port `{{extra_port}}` in addition to the standard port 22. Both ports must appear in the effective daemon configuration, the configuration must be syntactically valid, and it must persist across reboots.

Port 22 must remain configured: do not remove or replace it.
