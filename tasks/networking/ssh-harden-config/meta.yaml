schema_version: 1
id: networking/ssh-harden-config
version: 1.0.0
domain: networking
weight: 15
estimated_minutes: 8
requires:
  - openssh-server
  - passwd
parameters:
  permit_root_login: one of [prohibit-password, "no"]
  match_user: seed-derived local account name
