# Networking task

Task: Configure the static IPv4 address `{{ip_address}}/{{prefix_len}}` on the network interface `{{lab_nic}}`, with DHCP disabled for that interface. The address must be active now and the configuration must persist across reboots.

Do not modify the configuration of the primary network interface or the default route, and do not modify or delete any configuration files you did not create.
