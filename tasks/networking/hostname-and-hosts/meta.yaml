schema_version: 1
id: networking/hostname-and-hosts
version: 1.0.0
domain: networking
weight: 5
estimated_minutes: 4
requires:
  - systemd
  - libc-bin
parameters:
  new_hostname: lab-<token>
  hosts_domain: lab<token>.example
  host_entries: two fictitious hosts with documentation-range IPv4 addresses
